<view class="a-class a-badge">
  <slot></slot>
  <view
        wx:if="{{ dot }}"
        class="a-badge-dot"></view>
  <view
        wx:elif="{{ value !== 0 }}"
        class="a-badge-number">{{ value > maxValue ? '...' : value }}
  </view>
</view>

