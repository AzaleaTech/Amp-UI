<image
       src="{{ src }}"
       class="a-class avatar {{ circle ? 'avatar-circle' : '' }} {{ border ? 'avatar-border': '' }}"
       style="width: {{ size }}rpx; height: {{ size }}rpx; border-color: {{ borderColor }}"
       bind:tap="handleClick"></image>

