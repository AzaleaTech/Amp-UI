<a-modal wx:if="{{ show }}">
  <view class="a-toast">
    <a-icon
            name="{{ iconName }}"
            color="{{ iconColor }}"></a-icon>
    <view class="a-toast-content">{{ toastData.content }}</view>
  </view>
</a-modal>

