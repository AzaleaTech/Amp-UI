<a-modal wx:if="{{ show }}">
  <view class="a-toast">
    <image
           src="./images/{{ toastData.type || 'success' }}.png"
           class="a-toast-img" />
    <view class="a-toast-content">{{ toastData.content }}</view>
  </view>
</a-modal>

