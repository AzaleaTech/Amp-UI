<view class="example-box">
  <view class="example-box-title">基础用法</view>
  <view class="example-box-content">
    <a-image src="{{ url }}" bind:click="handlePreview" />
  </view>
  <view class="example-box-title">自定义预览</view>
  <view class="example-box-content">
    <a-image src="{{ url }}" bind:click="handlePreviewImgs" />
  </view>
  <view class="example-box-title">容错处理</view>
  <view class="example-box-content">
    <a-image src="xxx" fallback="{{ defaultUrl }}" />
  </view>
</view>