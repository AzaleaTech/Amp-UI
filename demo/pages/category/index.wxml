<view class="category">
  <view class="category-title">WXCOM组件库DEMO</view>
  <a-form
          class="category-form"
          label-width="600">
    <a-form-item
                 wx:for="{{ components }}"
                 wx:key="index"
                 label="{{ item.label }}"
                 footerArrow
                 data-label="{{ item.label }}"
                 data-url="{{ item.url }}"
                 bind:click="handleClickFormItem"></a-form-item>
  </a-form>
</view>

