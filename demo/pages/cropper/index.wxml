<button
        wx:if="{{ !isCropper }}"
        bind:tap="handleClick">裁剪图片
</button>
<cropper
         wx:else
         cutScale="{{ 690 / 400 }}"
         imgUrl="{{ image }}"
         type="uploadImage"
         bind:cancel="handleCancel"
         bind:save="handleSave" />

