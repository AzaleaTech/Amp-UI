<a-button
          type="primary"
          size="mini"
          width="184"
          disabled="{{ remainingTime > 0 || disabled }}"
          bind:clickButton="handleClickButton">
  {{ content }}{{ remainingTime ? remainingTime : '' }}
</a-button>

