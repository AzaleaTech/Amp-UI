<view
      class="a-class radio"
      catch:tap="handleChange">
  <view class="radio-dot {{ checked ? 'radio-dot-checked' : '' }}  {{ disabled ? 'radio-dot-disabled': '' }} radio-dot-{{ position }}"></view>
  <view class="radio-title">{{ value }}</view>
</view>

